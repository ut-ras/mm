# Common things for the projects in `software/`.
# Set SW_DIR before including.

CONTRIB_DIR = $(SW_DIR)/contrib
SHARED_COMPONENTS_DIR = $(SW_DIR)/components

include $(CONTRIB_DIR)/contrib.vars
include $(SHARED_COMPONENTS_DIR)/components.vars

include $(IDF_PATH)/make/project.mk
